package br.com.desafio.dio.dominio;

public class ContaCorrenteImpl extends ContaCorrente {
    public ContaCorrenteImpl(String numero, Cliente cliente, double limite) {
        super(numero, cliente, limite);
    }
}
